<% if @answer.errors.present? %>
   <% @answer.errors.full_messages.each do |message| %>
   $('.answer-error').html('<%= j message %>')
   <% end %>
<% else %>
    $('.new_answer_form').hide()
    $('.answers').append('<%= j render @answer, locals: {answer: @answer} %>');
    $('#clear').val(''); 
<% end %>